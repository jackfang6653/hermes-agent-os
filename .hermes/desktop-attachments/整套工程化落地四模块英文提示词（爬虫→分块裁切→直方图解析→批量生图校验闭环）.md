# 整套工程化落地四模块英文提示词（爬虫→分块裁切→直方图解析→批量生图校验闭环）

## 模块 1：网页爬虫抓取详情页 指令（适配爬虫 / Playwright/Puppeteer）

```Plain Text
Use headless browser to access the target product detail page URL, capture full page screenshot, identify and split the page into predefined functional modules by visual layout and semantic structure: Hero Banner Module, Main Product Carousel Module, Close-up Macro Module, Scene Lifestyle Module, Parameter Infographic Module, Texture Detail Module, Text Copy Block, Divider & Decor Element. Record each module’s bounding box coordinate (x1, y1, x2, y2), layer Z-index and vertical scroll order, crop each module into independent PNG image files with unique ModuleID naming, save all cropped images to designated folder and output a mapping JSON of ModuleID, type and file path. Ignore floating pop-ups, advertising layers and irrelevant floating buttons during segmentation.
```

## 模块 2：单模块图像 ACR 直方图 \+ 视觉 DNA 解析指令（GPT\-4V / 视觉模型专用）

```Plain Text
Input a single cropped module image, execute dual analysis: Adobe Camera Raw histogram quantitative analysis + visual element DNA decomposition.
1. Histogram & ACR core parameters: luminance pixel distribution trend, RGB three-channel peak position and overlap ratio, shadow clipping state, highlight clipping state, gamma value, Exposure, Contrast, Highlights, Shadows, Whites, Blacks, Texture, Clarity, Dehaze, Vibrance, Saturation, Color Temperature(K), Tint, 5 anchor points of Tone Curve.
2. Visual style dimension: shot type, composition structure, primary light & fill light direction and ratio, foreground/midground/background layering depth, main color / auxiliary color / accent color HEX codes and area proportion, sharpening intensity, vignette range, background blur strength.
3. For text-containing modules: extract full copy content, font family, font weight, font size, line spacing, paragraph spacing, character spacing, text HEX color, alignment mode and grid layout margin padding.
Output all analysis results strictly in standardized JSON format without redundant descriptive text.
```

## 模块 3：全局风格锁死 \+ 分模块批量生成约束 Prompt（SD / MJ / 文生图通用）

### Positive Prompt

```Plain Text
Generate product detail page corresponding module material, strictly inherit the ModuleID matched ACR histogram fingerprint and all ACR numerical parameters from benchmark database; lock luminance distribution, RGB channel histogram, shadow/highlight clipping status, tone curve, fixed white balance temperature and tint; keep consistent lighting direction, composition frame, color palette proportion and layering hierarchy with the benchmark module. The whole page follows global unified temperature baseline, contrast deviation between any two modules shall not exceed 15, saturation deviation shall not exceed 10, no independent color cast or exposure drift of single block. Text area adopts unified brand font system, fixed spacing and color specification.
```

### Negative Prompt

```Plain Text
histogram offset, highlight blowout, dead black shadow, RGB channel color skew, inconsistent white balance among modules, random exposure value, mismatched contrast and saturation, disordered light source direction, fragmented page style, tone curve distortion, extra clipping area, out-of-spec font and text color, messy typesetting spacing
```

## 模块 4：生成后自动校验 \+ 风格库迭代更新指令

```Plain Text
Automatically crawl and segment the newly generated detail page again, extract histogram and visual parameters of each module one more time, calculate deviation value between new parameters and benchmark preset. If deviation exceeds the threshold, mark abnormal items and auto-correct the corresponding parameter group in the module style library; accumulate multiple batches of qualified module data, extract common rules to form category-level unified ACR preset and page layout specification; record the marketing, consumer psychology and visual communication reasons for each parameter setting, realize continuous self-optimization and version iteration of the entire detail page style system, finally archive updated style database with version number.
```

## 统一标准 JSON 数据结构（全链路通用入库格式）

```json
{
  "PageBenchmarkID": "DetailPage_Brand_SKU_Version",
  "GlobalGlobalConstraint": {
    "BaseTemperatureK": 4800,
    "MaxContrastOffset": 15,
    "MaxSaturationOffset": 10
  },
  "ModuleLibrary": [
    {
      "ModuleID": "M001",
      "ModuleType": "HeroBanner",
      "FilePath": "./crop/M001.png",
      "BoundingBox": [12,45,1180,620],
      "HistogramFingerprint": {
        "LuminanceBias": "MidtoneMain",
        "RGBPeak": {"R":29,"G":41,"B":33},
        "ClipShadow": false,
        "ClipHighlight": false,
        "Gamma": 2.2
      },
      "ACRParams": {
        "Exposure": 0.1,
        "Contrast": 24,
        "Highlights": -38,
        "Shadows": 26,
        "Whites": -14,
        "Blacks": 31,
        "Temperature_K": 4800,
        "Tint": 3
      },
      "VisualDNA": {
        "ShotType": "MediumShot",
        "LightMode": "SoftFrontKeyLight",
        "MainColorHex": "#f7f5f0",
        "AuxColorHex": "#d6d2cb",
        "AccentColorHex": "#222222"
      },
      "TextParams": {},
      "LastGenerateDeviation": 2.3,
      "Version": "V1.0"
    }
  ]
}
```

## 一行极简总控 Prompt（可直接作为系统提示词置顶）

```Plain Text
Auto crawl target product detail page, split into standard modules and crop each block, analyze every cropped image with ACR histogram full parameter extraction plus composition lighting color text layout decomposition, store all data into classified module JSON style library; when generating new page materials, each module calls matched preset to lock histogram, tone, color and layout, restrict global page parameter deviation range; re-inspect generated page for parameter error, automatically update and iterate the style database, output standardized structured data throughout the whole process.
```

如需适配 **ComfyUI 节点式工作流拆分、接口入参字段定义、本地 Python 脚本注释版提示词**，可以告诉我对应工具，我直接定制适配版本。

> （注：部分内容可能由 AI 生成）
